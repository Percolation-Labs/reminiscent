# Phoenix Auth Secrets - now managed via ExternalSecret (phoenix-secrets-external.yaml)
# This file is kept for documentation but should not be applied directly.
#
# The secrets are stored in AWS SSM Parameter Store:
#   /rem/phoenix/secret       - PHOENIX_SECRET
#   /rem/phoenix/admin-secret - PHOENIX_ADMIN_SECRET
#
# See: phoenix-secrets-external.yaml for the ExternalSecret that syncs these.
---
# DEPRECATED - Use phoenix-secrets-external.yaml instead
# apiVersion: v1
# kind: Secret
# metadata:
#   name: phoenix-secrets
#   namespace: rem
# type: Opaque
# stringData:
#   PHOENIX_SECRET: "generate-secure-secret"
#   PHOENIX_ADMIN_SECRET: "generate-secure-secret"
